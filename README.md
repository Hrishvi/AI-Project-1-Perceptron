# README

Train a perceptron using only raw Python.

## Features

- Train a perceptron using only raw Python
- Save trained models with `pickle`
- Load and test saved models

## Supported Logic Gates

- AND gate
- OR gate
- NOR gate

## Model Storage

Trained models are stored inside the `Trained_Models` folder.

---

*(Used AI for grammar)*
